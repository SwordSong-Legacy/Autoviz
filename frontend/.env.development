# Local development: use backend on localhost
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000
# Use Next.js proxy for API (same-origin) so cookies/auth work for images
NEXT_PUBLIC_USE_API_PROXY=true
